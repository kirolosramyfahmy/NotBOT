# Database Credentials
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DB=sentinel_core

# Application Config
PROJECT_NAME="Sentinel"

# Database URL for Local Development (outside Docker)
# For Docker, it is constructed automatically in docker-compose
DATABASE_URL=postgresql+asyncpg://postgres:postgres@localhost:5432/sentinel_core
